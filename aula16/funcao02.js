//Caso os parâmetros não sejam passados, é possível definir um valor padrão para a variável na própria function. function nome(param=valpadrao)

function soma(n1=0, n2){
    return n1 + n2
}

console.log(soma(223,23))